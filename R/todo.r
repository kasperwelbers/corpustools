## finish addint is_tcorpus and possible shattered tCorpus compatibility to functions

## in search_features, make it so that multiple words found with the same query are all coded, but add a column indicating the unique occurence of that query (so that the number of hits can be counted appropriately)
